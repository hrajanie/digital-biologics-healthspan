# Healthspan implementation
All amounts in GameState and finance APIs are integer USD cents. Share quantities are exact rational strings, not floating ownership percentages. Only core/engine applies effects to authoritative state. World/economy helpers are pure. UI dispatches Actions and reads GameView; never invent finances, clinical outcomes or score. No live AI, patient encounters, real transaction terms or private research in shipped assets. Preserve expected lifetime score, improving comparator, actual initiation cutoff, continued-care requirements, and control-loss failure. Do not alter dependencies or shared contracts without coordinating with lead.
